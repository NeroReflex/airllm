# Install PyTorch with CUDA 12.8 support:
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
# (CUDA 12.8 is the latest release with published PyTorch wheels; CUDA 13.x
#  wheels will be available here once PyTorch publishes them.)
torch>=2.10
transformers==4.57.1
accelerate>=1.13
safetensors>=0.7
huggingface_hub>=0.34,<1.0
hf_xet>=1.4.2; platform_machine == "x86_64" or platform_machine == "amd64" or platform_machine == "AMD64" or platform_machine == "arm64" or platform_machine == "aarch64"
tqdm>=4.65
bitsandbytes>=0.49
sentencepiece>=0.2
openai-harmony>=0.0.8
httpx>=0.27
