# All package metadata and dependencies are declared in pyproject.toml.
# This file exists only for backward compatibility with tools that call
# `python setup.py install` directly.
from setuptools import setup
setup()
